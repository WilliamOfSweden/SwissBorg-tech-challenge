import React from 'react'

import * as chartWrapperStyles from './chartWrapperStyles.module.css'

type Props = {}

const ChartWrapper = (props: Props) => {
  return <div className={chartWrapperStyles.chartWrapper}></div>
}

export default ChartWrapper
