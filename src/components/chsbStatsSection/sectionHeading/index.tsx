import React from 'react'

import * as sectionHeadingStyles from './sectionHeadingStyles.module.css'

const SectionHeading = () => (
  <h2 className={sectionHeadingStyles.sectionHeading}>
    A breakdown of CHSB’s circulating supply
  </h2>
)

export default SectionHeading
